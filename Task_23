#Task 23: Find Second Largest
def find_second_largest():
    # Input: Taking a list of integers from the user
    numbers = list(map(int, input("Enter a list of numbers separated by spaces: ").split()))
    
    # Process: Find the second largest number
    unique_numbers = list(set(numbers))  # Remove duplicates
    unique_numbers.sort()  # Sort the list in ascending order
    second_largest = unique_numbers[-2]  # Get the second last element

    # Output: Print the second largest number
    print(f"The second largest number is: {second_largest}")

# Call the function
find_second_largest()

Enter a list of numbers separated by spaces:  10 20 20 30
The second largest number is: 20
